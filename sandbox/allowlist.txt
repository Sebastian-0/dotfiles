# Domains reachable from inside the sandbox.
# One per line. Blank lines and '# comments' are ignored.
# The firewall also always allows what Claude Code itself needs
# (api.anthropic.com, the console.anthropic.com/claude.ai login endpoints,
# statsig.*, sentry.io, registry.npmjs.org) plus GitHub's meta CIDR ranges --
# edit this file to add anything beyond that. Takes effect on the next
# `claudesafe` run. Ignored entirely under `claudesafe --strict`.

# Python packaging
pypi.org
files.pythonhosted.org

# GitHub (beyond api.github.com which is already covered by the meta ranges)
raw.githubusercontent.com
codeload.github.com
objects.githubusercontent.com
github.blog

# TODO Use wildcards here when we have a better allow list (*.blob.core.windows.net)
# Github actions artifacts
productionresultssa17.blob.core.windows.net
productionresultssa8.blob.core.windows.net
productionresultssa5.blob.core.windows.net
azureopendatastorage.blob.core.windows.net

# Anthropic docs (useful during Claude sessions)
docs.anthropic.com

# Debian apt mirrors (the base image is node:20 -> Debian bookworm)
deb.debian.org
security.debian.org
archive.debian.org

# Ubuntu apt mirrors
archive.ubuntu.com
security.ubuntu.com
ports.ubuntu.com
keyserver.ubuntu.com

# Intui resources
core.hw.intuicell.com
jetson-1.hw.intuicell.com

# Nvidia mirrors
developer.download.nvidia.com

# Pytorch mirror
download.pytorch.org
